name: Deploy docker image to Kubernetes

on:
  push:
    branches: [ main ]
    tags: [ 'v*.*.*' ]
    paths-ignore:
      - 'deploy-prometheus.sh'
      - 'ingress-grafana.yaml'
      - '**.md'
      - '.github/**'

